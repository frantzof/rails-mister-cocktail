<h2><%= @cocktail.name %></h2>
<%= cl_image_tag @cocktail.photo.key, height: 300, width: 400, crop: :fill %>

<ul class="list-group">
  <% @cocktail.doses.each do |dose| %>
    <li class="list-group-item"><%= dose.description %> <%= "#{dose.ingredient.name} | " %><%= link_to "🗑 Remove", cocktail_dose_path(@cocktail), method: :delete, data: { confirm: "Are you sure?" } %></li>
  <% end %>
</ul>

<%= link_to "Let's submit the doses", new_cocktail_dose_path(@cocktail) %><br>
<%= link_to "Back to all cocktails", cocktails_path %>
